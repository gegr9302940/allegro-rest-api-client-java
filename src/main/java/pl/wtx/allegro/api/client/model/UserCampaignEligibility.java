/*
 * Allegro REST API
 * https://developer.allegro.pl/about  Documentation is generated from [this OpenAPI 3.0 specification file](https://developer.allegro.pl/swagger.yaml). To start working with our API, you can also check our [official Allegro REST API public collection](https://www.postman.com/allegro-rest-api/allegro-rest-api/collection/4puh6ls/allegro-rest-api) in Postman.
 *
 * The version of the OpenAPI document: latest
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package pl.wtx.allegro.api.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import pl.wtx.allegro.api.client.model.CampaignRefusalReason;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import pl.wtx.allegro.api.client.invoker.JSON;

/**
 * Summary of user eligibility for participation in the campaign.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.13.0")
public class UserCampaignEligibility {
  public static final String SERIALIZED_NAME_ELIGIBLE = "eligible";
  @SerializedName(SERIALIZED_NAME_ELIGIBLE)
  @javax.annotation.Nonnull
  private Boolean eligible;

  public static final String SERIALIZED_NAME_REFUSAL_REASONS = "refusalReasons";
  @SerializedName(SERIALIZED_NAME_REFUSAL_REASONS)
  @javax.annotation.Nonnull
  private List<CampaignRefusalReason> refusalReasons = new ArrayList<>();

  public UserCampaignEligibility() {
  }

  public UserCampaignEligibility eligible(@javax.annotation.Nonnull Boolean eligible) {
    this.eligible = eligible;
    return this;
  }

  /**
   * Information whether user is eligible to participate in this campaign.
   * @return eligible
   */
  @javax.annotation.Nonnull
  public Boolean getEligible() {
    return eligible;
  }

  public void setEligible(@javax.annotation.Nonnull Boolean eligible) {
    this.eligible = eligible;
  }


  public UserCampaignEligibility refusalReasons(@javax.annotation.Nonnull List<CampaignRefusalReason> refusalReasons) {
    this.refusalReasons = refusalReasons;
    return this;
  }

  public UserCampaignEligibility addRefusalReasonsItem(CampaignRefusalReason refusalReasonsItem) {
    if (this.refusalReasons == null) {
      this.refusalReasons = new ArrayList<>();
    }
    this.refusalReasons.add(refusalReasonsItem);
    return this;
  }

  /**
   * Information why user is not able to participate in the campaign.
   * @return refusalReasons
   */
  @javax.annotation.Nonnull
  public List<CampaignRefusalReason> getRefusalReasons() {
    return refusalReasons;
  }

  public void setRefusalReasons(@javax.annotation.Nonnull List<CampaignRefusalReason> refusalReasons) {
    this.refusalReasons = refusalReasons;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UserCampaignEligibility userCampaignEligibility = (UserCampaignEligibility) o;
    return Objects.equals(this.eligible, userCampaignEligibility.eligible) &&
        Objects.equals(this.refusalReasons, userCampaignEligibility.refusalReasons);
  }

  @Override
  public int hashCode() {
    return Objects.hash(eligible, refusalReasons);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UserCampaignEligibility {\n");
    sb.append("    eligible: ").append(toIndentedString(eligible)).append("\n");
    sb.append("    refusalReasons: ").append(toIndentedString(refusalReasons)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("eligible");
    openapiFields.add("refusalReasons");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("eligible");
    openapiRequiredFields.add("refusalReasons");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to UserCampaignEligibility
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!UserCampaignEligibility.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in UserCampaignEligibility is not found in the empty JSON string", UserCampaignEligibility.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!UserCampaignEligibility.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `UserCampaignEligibility` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : UserCampaignEligibility.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the json data is an array
      if (!jsonObj.get("refusalReasons").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `refusalReasons` to be an array in the JSON string but got `%s`", jsonObj.get("refusalReasons").toString()));
      }

      JsonArray jsonArrayrefusalReasons = jsonObj.getAsJsonArray("refusalReasons");
      // validate the required field `refusalReasons` (array)
      for (int i = 0; i < jsonArrayrefusalReasons.size(); i++) {
        CampaignRefusalReason.validateJsonElement(jsonArrayrefusalReasons.get(i));
      };
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!UserCampaignEligibility.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'UserCampaignEligibility' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<UserCampaignEligibility> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(UserCampaignEligibility.class));

       return (TypeAdapter<T>) new TypeAdapter<UserCampaignEligibility>() {
           @Override
           public void write(JsonWriter out, UserCampaignEligibility value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public UserCampaignEligibility read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of UserCampaignEligibility given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of UserCampaignEligibility
   * @throws IOException if the JSON string is invalid with respect to UserCampaignEligibility
   */
  public static UserCampaignEligibility fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, UserCampaignEligibility.class);
  }

  /**
   * Convert an instance of UserCampaignEligibility to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

